[package]
name = "scanner-cargo-deny"
version = "1.0.0"
edition = "2021"
description = "cargo-deny (advisories+license+bans) scanner wrapper for Techanv Warden"

[[bin]]
name = "scanner-cargo-deny"
path = "src/main.rs"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
lto = true
